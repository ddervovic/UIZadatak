<?xml version="1.0" encoding="utf-8"?>
<merge>
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:id="@+id/input_text"
        android:layout_alignParentBottom="true">

        <EditText
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:id="@+id/message_view_user_input"
            android:inputType="textMultiLine"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            />

        <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/send"
            android:adjustViewBounds="true"
            android:maxHeight="40dp"
            android:maxWidth="40dp"
            android:layout_alignTop="@+id/message_view_user_input"
            android:layout_toRightOf="@+id/message_view_user_input"
            android:layout_toEndOf="@+id/message_view_user_input"
            android:scaleType="fitXY"
            android:cropToPadding="false"
            android:clickable="true"
            android:baselineAlignBottom="false" />
    </RelativeLayout>

</merge>
